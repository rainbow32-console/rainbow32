{
    "scripts": {
        "watch": "esbuild --watch --bundle --outdir=./public/dist --minify --sourcemap ./src/index.ts",
        "build": "esbuild --bundle --outdir=./public/dist --minify --sourcemap ./src/index.ts && tsx package.ts"
    },
    "dependencies": {
        "@types/express": "^4.17.17",
        "esbuild-wasm": "^0.17.18",
        "library": "../library/dist"
    }
}

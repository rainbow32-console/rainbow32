{
    "scripts": {
        "watch:client": "esbuild --watch --bundle --outdir=./client/public/dist --minify --sourcemap ./client/src/index.ts",
        "build:client": "esbuild --bundle --outdir=./client/public/dist --minify --sourcemap ./client/src/index.ts"
    },
    "dependencies": {
        "@types/body-parser": "^1.19.2",
        "@types/express": "^4.17.17",
        "@types/node": "^18.15.11",
        "body-parser": "^1.20.2",
        "express": "^4.18.2",
        "library": "../library/dist"
    }
}
